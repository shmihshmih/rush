import { HideAnswerPipe } from './hide-answer.pipe';

describe('HideAnswerPipe', () => {
  it('create an instance', () => {
    const pipe = new HideAnswerPipe();
    expect(pipe).toBeTruthy();
  });
});
